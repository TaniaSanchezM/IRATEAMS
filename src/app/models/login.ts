export class Login {
    public userId:        number;
    public userCredentials: string;
    public password:        string;
    constructor(){
        
    }
    // get userId() {
    //     return this._userId || Number(sessionStorage.getItem('userId'));
    // }
    // set userId(value: number) {
    //     if (value) {
    //         sessionStorage.setItem('userId', String(value))
    //     } else {
    //         sessionStorage.removeItem('userId');
    //     }
    //     this._userId = value;
    // }
}
